# This file contains all the environment variables needed to run the application.
# Copy this file to .env and fill in the values.

# -- DATABASE --
# The connection string for the Drizzle ORM. For local dev, this points to a SQLite file.
DATABASE_URL="file:./db/local.sqlite"

# -- API & WEB --
# The public URL for the web app to communicate with the API server.
NEXT_PUBLIC_API_URL="http://localhost:8000"

# -- THIRD PARTY SERVICES --
# Example API key for a service like Nike's public API.
NIKE_API_KEY="your_nike_api_key_here"